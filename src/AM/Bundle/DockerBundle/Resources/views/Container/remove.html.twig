{% extends 'default/index.html.twig' %}


{% block title %}{% trans %}Remove a container{% endtrans %}{% endblock %}

{% block content %}
<div class="page-header">
    <h1>{% trans %}Remove a container{% endtrans %}</h1>
</div>
    <a class="btn btn-default" href="{{ path('am_docker_container_details', {'id':container.id}) }}">{% trans %}Cancel{% endtrans %}</a>
{% if form is defined %}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
    {{ form(form) }}
{% else %}
    <div class="alert alert-danger" role="alert">
        <p><i class="glyphicon glyphicon-exclamation-sign"></i> <strong>{% trans %}LittleShip could not connect to your Docker…{% endtrans %}</strong></p>
        {% if error is defined %}
        <p>{{ error }}</p>
        {% endif %}
    </div>
{% endif %}
{% endblock %}
